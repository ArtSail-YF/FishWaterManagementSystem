# 🏗️ 前端架构设计规范 - Service/Type 三层架构

## 📋 概述

本文档详细说明水产管理系统前端项目的Service/Type三层架构设计，包括各层职责、类型流转逻辑、命名规范等。

## 🏛️ 架构分层设计

### 整体架构图
```
┌─────────────────────────────────────────┐
│             页面层 (Pages)               │  ← 使用Model层
├─────────────────────────────────────────┤
│            服务层 (Services)             │  ← 使用Type层
├─────────────────────────────────────────┤
│            模型层 (Models)               │  ← 扩展Type层
├─────────────────────────────────────────┤
│            类型层 (Types)                │  ← 基础类型定义
└─────────────────────────────────────────┘
```

## 📁 目录结构规范

### 类型层 (src/types/)
```
src/types/
├── common.ts        # 通用类型定义 (BaseResponse, PaginationParams等)
├── enum.ts          # 枚举类型定义 (PlanTypeEnum, WarningTypeEnum等)
├── model.ts         # 数据模型定义 (ProductionPlan, WarningRule等)
├── api.ts           # API接口类型定义 (请求/响应参数类型)
└── index.ts         # 统一导出
```

### 模型层 (src/models/)
```
src/models/
├── production.ts    # 生产管理前端模型
├── warning.ts       # 预警中心前端模型
├── water.ts         # 水质监测前端模型
├── pond.ts          # 塘口管理前端模型
└── index.ts         # 统一导出
```

### 服务层 (src/services/)
```
src/services/
├── api/             # 业务API接口
│   ├── production.ts    # 生产管理API
│   ├── warning.ts       # 预警中心API
│   ├── water.ts         # 水质监测API
│   ├── pond.ts          # 塘口管理API
│   └── index.ts         # 统一导出
└── ant-design-pro/  # 框架相关API (用户认证等)
```

## 🔧 各层职责说明

### 1. 类型层 (Types Layer)
**职责**: 基础类型定义，映射后端DTO，确保类型安全

#### 核心类型定义
```typescript
// 基础响应结构 (common.ts)
export interface BaseResponse<T = any> {
  code: number;       // 200-成功，其他-失败
  message: string;    // 描述信息
  data: T;           // 业务数据
  description?: string;
}

// 分页参数 (common.ts)
export interface PaginationParams {
  current?: number;   // 当前页码
  pageSize?: number;  // 每页大小
  keyword?: string;   // 搜索关键词
  [key: string]: any; // 其他查询条件
}

// 基础实体 (common.ts)
export interface BaseEntity {
  id: string;         // 主键ID
  createTime?: string; // 创建时间
  updateTime?: string; // 更新时间
  isDeleted?: number;  // 逻辑删除：0-正常，1-删除
}
```

#### 业务类型定义
```typescript
// 生产计划模型 (model.ts)
export interface ProductionPlan extends BaseEntity {
  planType: PlanTypeEnum;      // 计划类型
  planName: string;            // 计划名称
  pondIds: string[];           // 塘口ID列表
  content?: any;               // 计划内容(JSON格式)
  startTime: string;           // 计划开始时间
  endTime: string;             // 计划结束时间
  status: PlanStatusEnum;      // 状态
}

// API接口类型 (api.ts)
export interface ProductionPlanParams extends PaginationParams {
  planType?: number;
  status?: number;
  approvalStatus?: number;
  keyword?: string;
  startTime?: string;
  endTime?: string;
}

export type ProductionPlanList = PaginationResponse<ProductionPlan>;
export type ProductionPlanDetail = BaseResponse<ProductionPlan>;
```

### 2. 模型层 (Models Layer)
**职责**: 前端专用数据模型，扩展业务功能，提供UI展示所需数据

#### 前端扩展模型
```typescript
// 生产计划前端模型 (models/production.ts)
export interface ProductionPlanModel extends ProductionPlan {
  // 前端扩展字段
  pondNames?: string[];          // 塘口名称列表（前端显示用）
  creatorName?: string;          // 创建人姓名
  approverName?: string;         // 审批人姓名
  planTypeLabel?: string;        // 计划类型标签
  statusLabel?: string;          // 状态标签
  
  // 业务计算字段
  progress?: number;             // 执行进度（0-100）
  taskCount?: number;            // 任务总数
  completedTaskCount?: number;   // 已完成任务数
  
  // 时间格式化字段
  startTimeFormatted?: string;   // 格式化开始时间
  endTimeFormatted?: string;     // 格式化结束时间
  createTimeFormatted?: string;  // 格式化创建时间
}
```

### 3. 服务层 (Services Layer)
**职责**: API接口封装，业务逻辑处理，数据转换

#### API接口实现
```typescript
// 生产管理API (services/api/production.ts)
import { request } from '@umijs/max';
import type {
  ProductionPlanParams,
  ProductionPlanList,
  ProductionPlanDetail,
  BaseResponse,
  ProductionPlan
} from '@/types';

/** 获取生产计划列表 GET /production/plan/list */
export async function getProductionPlanList(
  params: ProductionPlanParams,
  options?: { [key: string]: any },
) {
  return request<ProductionPlanList>('/production/plan/list', {
    method: 'GET',
    params: { ...params },
    ...(options || {}),
  });
}

/** 创建生产计划 POST /production/plan/create */
export async function createProductionPlan(
  body: Omit<ProductionPlan, 'id' | 'createTime' | 'updateTime' | 'isDeleted'>,
  options?: { [key: string]: any },
) {
  return request<BaseResponse<string>>('/production/plan/create', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    data: body,
    ...(options || {}),
  });
}
```

### 4. 页面层 (Pages Layer)
**职责**: UI展示，用户交互，调用Service层获取数据

#### 页面组件使用
```typescript
// 页面组件 (pages/production/plan/index.tsx)
import { getProductionPlanList } from '@/services/api/production';
import type { ProductionPlanModel } from '@/models';

const ProductionPlanPage: React.FC = () => {
  const [plans, setPlans] = useState<ProductionPlanModel[]>([]);
  
  const loadData = async () => {
    const response = await getProductionPlanList({
      current: 1,
      pageSize: 10
    });
    
    if (response.code === 200) {
      // 数据转换：Type层 → Model层
      const planModels: ProductionPlanModel[] = response.data.records.map(plan => ({
        ...plan,
        planTypeLabel: getPlanTypeLabel(plan.planType),
        progress: calculateProgress(plan),
        startTimeFormatted: formatDate(plan.startTime)
      }));
      
      setPlans(planModels);
    }
  };
  
  return (
    <div>
      <Table dataSource={plans} columns={columns} />
      <Progress percent={plan.progress} />
    </div>
  );
};
```

## 🔄 类型流转逻辑

### 数据流转示意图
```
后端API → Type层 → Service层 → Model层 → 页面层
    ↓        ↓         ↓         ↓        ↓
  DTO    基础类型   API封装  前端扩展    UI展示
```

### 具体流转示例
```typescript
// 1. Type层：定义基础类型 (映射后端DTO)
interface ProductionPlan {
  id: string;
  planName: string;
  startTime: string;
  status: number;
}

// 2. Service层：API调用 (使用Type层类型)
const response = await getProductionPlanList(params); // → BaseResponse<ProductionPlan[]>

// 3. Model层：数据转换 (扩展前端专用字段)
const planModel: ProductionPlanModel = {
  ...response.data,
  startTimeFormatted: formatDate(response.data.startTime), // 前端格式化
  statusLabel: getStatusLabel(response.data.status),       // 状态标签
  progress: calculateProgress(response.data)               // 业务计算
};

// 4. 页面层：使用扩展数据 (UI展示)
<Progress percent={planModel.progress} />
<Badge status={planModel.statusLabel} />
```

## 🎯 命名规范

### 类型命名规范
- **接口**: 使用PascalCase，如 `ProductionPlan`
- **枚举**: 使用PascalCase + Enum后缀，如 `PlanTypeEnum`
- **类型别名**: 使用PascalCase，如 `ProductionPlanList`
- **参数类型**: 使用PascalCase + Params后缀，如 `ProductionPlanParams`

### 文件命名规范
- **类型文件**: 使用小写，如 `common.ts`, `model.ts`
- **模型文件**: 使用业务模块名，如 `production.ts`, `warning.ts`
- **服务文件**: 使用业务模块名，如 `production.ts`, `water.ts`

## 🔧 路径规范化

### API路径规范
```typescript
// ❌ 错误：包含冗余前缀
return request<BaseResponse>('/api/production/plan/list', {

// ✅ 正确：简洁路径（前端拦截器自动添加/api/前缀）
return request<BaseResponse>('/production/plan/list', {
```

### 前端拦截器配置
```typescript
// 拦截器自动添加/api/前缀
request.interceptors.request.use((config) => {
  if (!config.url.startsWith('http') && !config.url.startsWith('/api')) {
    config.url = '/api' + config.url;
  }
  return config;
});
```

## ✅ 类型安全优势

### 编译时类型检查
```typescript
// ❌ 错误的参数类型会被TypeScript捕获
getProductionPlanList('invalid'); // 编译错误

// ✅ 正确的参数类型
getProductionPlanList({ current: 1, pageSize: 10 }); // 通过
```

### 智能代码提示
```typescript
// IDE会提供完整的类型提示
getProductionPlanList({
  current: 1,        // ← 智能提示
  pageSize: 10,      // ← 智能提示  
  keyword: '',       // ← 智能提示
  planType: 1,       // ← 智能提示
});
```

## 🚀 扩展性设计

### 新增业务模块流程
1. **Type层**: 在`types/model.ts`中添加数据模型
2. **Model层**: 在`models/`中创建前端扩展模型
3. **Service层**: 在`services/api/`中实现API接口
4. **页面层**: 使用Model层数据进行UI展示

### 类型更新传播
当后端API变更时，只需更新Type层定义，所有依赖该类型的代码都会自动获得类型检查。

## 📊 架构优势总结

| 优势 | 说明 |
|------|------|
| ✅ 职责分离 | 各层职责明确，避免代码混乱 |
| ✅ 类型安全 | 完整的TypeScript支持，编译时错误检测 |
| ✅ 易于维护 | 统一管理类型定义，修改影响范围可控 |
| ✅ 扩展性强 | 新增业务模块有清晰的实现路径 |
| ✅ 开发效率 | IDE智能提示，减少手动类型检查 |
| ✅ 代码复用 | 类型定义可跨模块复用 |

## 🔍 最佳实践

### 1. 类型导入规范
```typescript
// ✅ 推荐：按需导入具体类型
import type { ProductionPlan, ProductionPlanParams } from '@/types';

// ❌ 避免：导入整个命名空间
import type * as API from 'ant-design-pro';
```

### 2. 数据转换规范
```typescript
// ✅ 推荐：在Service层或Model层进行数据转换
const convertToModel = (data: ProductionPlan): ProductionPlanModel => ({
  ...data,
  planTypeLabel: getPlanTypeLabel(data.planType),
  progress: calculateProgress(data)
});

// ❌ 避免：在页面层进行复杂的数据处理
```

### 3. 错误处理规范
```typescript
// ✅ 推荐：统一的错误处理
try {
  const response = await getProductionPlanList(params);
  if (response.code === 200) {
    // 处理成功数据
  } else {
    // 处理业务错误
    message.error(response.message);
  }
} catch (error) {
  // 处理网络错误
  message.error('网络请求失败');
}
```

---

**文档版本**: v1.0  
**最后更新**: 2026-04-16  
**维护者**: 前端架构团队