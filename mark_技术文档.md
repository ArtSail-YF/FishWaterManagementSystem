# GIS“一张图”管理页面设计方案

该方案旨在为老板提供一个直观的、基于地图的基地和塘口管理视图，通过“一张图”实现全局监控和快速定位。

## 1. 页面布局设计

页面采用经典的三栏式布局，确保信息展示清晰且操作便捷：

- **顶部 (可选): 全局状态统计条**

  - **功能**: 实时汇总所有基地的运行状态。
  - **指标**: 🟢 正常运行基地数 / 🟡 待办任务数 / 🔴 预警报警数。
  - **交互**: 点击对应状态可快速筛选地图上的标记点。
- **左侧 (20%-30% 宽度): 基地/塘口筛选面板**

  - **搜索栏**: 支持按基地名称、塘口编号快速搜索。
  - **筛选器**: 按区域（如：华东区、华南区）、状态（正常、预警）进行过滤。
  - **基地列表**:
    - 展示基地概览：名称、当前主要水质指标（如溶氧量）、状态图标。
    - **联动交互**: 点击列表项，地图自动平移（PanTo）至该基地，并弹出详细信息气泡。
- **中央 (70%-80% 宽度): 交互式地图 (高德地图)**

  - **底图控制**: 支持标准地图、卫星地图切换（方便查看实际地形）。
  - **图层管理**: 可勾选显示“基地分布”、“塘口边界”、“设备位置”等图层。
  - **标记点 (Markers)**:
    - 使用自定义图标区分不同状态的基地。
    - 鼠标悬停显示简要信息，点击打开“信息窗体 (InfoWindow)”。
  - **信息窗体**: 展示基地实时监控画面（如有）、核心传感器数据、今日操作记录统计。

---

## 2. 技术实现逻辑

### 前端技术栈

- **框架**: React (Ant Design Pro)
- **地图库**: `@amap/amap-jsapi-loader` (高德地图 JS API 2.0)
- **组件库**: Ant Design (Card, List, Input, Badge, Tag)

#### 地图库

* `@amap/amap-jsapi-loader` **是一个**  **纯前端 npm 包** **；**
* **它的作用是：** **在用户浏览器中动态加载高德地图的 JavaScript SDK** **；**

关于key的获取 :

不推荐：前端直接写死 Key

通过 后端代理的方式 ,如 **前端请求你的后端接口：**`GET /api/map-config`

### 核心代码结构

1. **`BaseDistribution/index.tsx`**: 主页面容器，负责管理全局状态（如当前选中的基地、筛选条件）。
2. **`components/GisStats.tsx`**: 顶部统计组件。
3. **`components/BaseList.tsx`**: 左侧列表组件，接收筛选后的数据并处理点击事件。
4. **`components/GisMap.tsx`**: 地图核心组件，负责高德地图初始化、标记点渲染、信息窗体弹出。

* **Card 文档** **：**[https://ant.design/components/card](https://ant.design/components/card)
* **Spin 文档** **：**[https://ant.design/components/spin](https://ant.design/components/spin)

### 需要实现的后端接口

获取基地汇总     包含 总基地数 , 正常,报警待检测的基地数

->**`GET /api/bases/summary`**

查询基地  通过 区域 状态 名称/标号(支持模糊查询)

查询基地状态  包含基本信息,传感器数据(ph,温度,氧气),近24小时日志

获取地图key

### 数据流转流程

1. **加载数据**: 进入页面时，从后端 API 获取所有基地及其坐标、状态数据。
2. **地图初始化**: 使用 `AMapLoader` 异步加载高德地图插件。
3. **渲染标记点**: 遍历基地数据，在地图上创建 `AMap.Marker`，并根据状态设置不同图标。
4. **联动机制**:
   - **左 -> 中**: 用户在左侧列表点击基地 -> 调用 `map.setCenter([lng, lat])` 并触发 `marker.emit('click')`。
   - **中 -> 左**: 用户在地图点击标记点 -> 更新全局状态 `selectedBaseId` -> 左侧列表高亮对应项。

---

## 3. 专家理由与业务价值

- **即时定位**: 相比于翻阅 Excel 或普通列表，地图模式能让管理层一秒看清“问题在哪里”。
- **区域统筹**: 老板通常管理跨地区的多个基地，通过缩放地图可以快速了解不同区域的整体运营状况。
- **管理闭环**: 从发现预警（地图红点）到查看详情（信息窗体）再到下达任务，形成高效的管理闭环。

# 水质监控页面设计方案

该页面旨在为养殖管理人员提供各基地、各塘口的实时水质数据监控与历史趋势分析，确保水质安全并能及时预警。

## 1. 页面布局设计

页面采用“全局统计 + 实时网格 + 详细分析”的三段式结构：

- **顶部 (10% 高度): 水质全局统计条**

  - **总览**: 监测中的塘口总数。
  - **状态统计**: 🟢 正常塘口数 / 🟡 低溶氧预警数 / 🔴 异常报警数。
  - **目的**: 快速评估整体水质安全状况。
- **中间 (40% 高度): 塘口实时监控网格 (Pond Card Grid)**

  - **搜索与筛选**: 支持按基地筛选、按状态过滤、搜索塘口名。
  - **实时卡片**: 每个卡片代表一个塘口，展示：
    - **核心指标**: 溶氧量 (DO)、水温 (Temp)、PH值。
    - **实时状态**: 使用不同颜色的边框或角标区分状态。
    - **趋势小图**: 指标旁的小箭头（上升/下降/平稳）表示近 1 小时的变化趋势。
  - **交互**: 点击卡片，下方“详细趋势分析”自动切换为该塘口数据。
- **底部 (50% 高度): 详细趋势分析与预警 (Analysis & Trends)**

  - **多维度图表 (ECharts)**:
    - 展示近 24 小时或近 7 天的溶氧量、水温、PH值曲线。
    - 支持指标切换显示。
  - **阈值设定展示**: 在图表上以虚线标出“安全预警线”和“紧急报警线”。
  - **最近报警记录**: 右侧列表展示该塘口最近 10 条报警信息及处理状态。

---

## 2. 技术实现逻辑

### 前端技术栈

- **框架**: React (Ant Design Pro)
- **图表库**: `echarts` 或 `@ant-design/plots` (建议使用 ECharts 处理大规模时序数据)
- **状态管理**: `useState` / `useMemo` 管理当前选中塘口和数据过滤逻辑。

### 核心代码结构

1. **`WaterQuality/index.tsx`**: 主页面容器。
2. **`components/WaterQualityStats.tsx`**: 顶部汇总组件。
3. **`components/PondCardGrid.tsx`**: 实时数据卡片网格。
4. **`components/QualityTrendChart.tsx`**: 趋势折线图组件。
5. **`components/RecentAlerts.tsx`**: 报警记录列表。

### 后端接口

**获取所有塘口基础信息 + 实时状态**

`GET /api/ponds`

* **返回字段：**`id`, `name`, `baseName`, `location`, `status`（normal/warning/alert）
* **✅ 用于顶部统计、筛选列表、地图标记、卡片网格**

**获取所有塘口最新水质数据**

`GET /api/ponds/latest`

* **返回字段：**`pondId`, `do`, `temp`, `ph`, `trend`（up/down/stable）, `timestamp`
* **✅ 用于实时卡片指标和趋势箭头**

**获取单个塘口历史数据（图表）**

`GET /api/ponds/{id}/history?range=last24h`

* **返回字段：时间序列数组 + 阈值（**`thresholds: { do: { warning: 5.0, alert: 3.0 } }`）
* **✅ 用于底部 ECharts 趋势图与预警线**

---

**获取某塘口最近报警记录**

`GET /api/ponds/{id}/alerts?limit=10`

* **返回字段：**`type`, `message`, `level`, `timestamp`, `resolved`
* **✅ 用于右侧报警列表**

### 数据流转流程

1. **实时拉取**: 通过 WebSocket 或长轮询（Polling）每分钟更新一次各塘口的实时指标。
2. **状态判定**: 根据预设的阈值，前端实时判定每个塘口的状态并渲染颜色。
3. **联动刷新**: 选中塘口改变时，图表组件自动重新加载该塘口的历史时序数据。

---

## 3. 专家理由与业务价值

- **精细化管理**: 从基地细化到具体每一个塘口，实现“全天候、无死角”监控。
- **预见性维护**: 通过趋势箭头和曲线，管理人员可以在水质恶化前（如溶氧量缓慢下降）就提前开启增氧设备。
- **异常追溯**: 结合历史曲线和报警记录，可以分析导致水质波动的规律（如投喂后溶氧下降、高温期波动等）。

# 生产计划页面设计方案

该页面旨在为养殖户和基地管理者提供一个清晰的生产作业视图，帮助其科学规划、按时执行并追踪各项生产任务（如投喂、用药、换水、捕捞等）。

## 1. 页面布局设计

页面采用“汇总统计 + 日历管理 + 任务列表”的组合布局，兼顾全局掌控与具体执行：

- **顶部 (10% 高度): 生产计划汇总条**

  - **核心指标**: 📅 今日待执行任务数 / ✅ 已完成任务数 / ⏳ 正在进行的计划数 / ⚠️ 逾期未完成数。
  - **目的**: 快速了解当天的生产饱和度与执行效率。
- **左侧 (60% 宽度): 任务执行与计划概览**

  - **任务执行中心 (Task Center)**:
    - 列表展示选定日期的所有任务及其执行状态。
    - **快捷操作**: 支持在列表中一键“确认执行”，或点击查看该计划的详细流程。
  - **生产计划日历 (Plan Calendar)**:
    - 采用可视化日历视图展示每月的生产任务分布。
    - **任务标记**: 在对应日期上展示不同颜色的小圆点（如：🔵 投喂、💊 用药、💧 换水）。
    - **交互**: 点击日期，上方“任务执行中心”自动切换为该日期的详细任务。
- **右侧 (40% 宽度): 计划执行时间轴 (Plan Timeline)**

  - **全周期视角**: 展示某个塘口从“投放鱼苗”到“成品捕捞”的全周期计划节点。
  - **进度跟踪**: 直观展示当前处于周期的哪个阶段（如：苗种期、生长期、育肥期）。
  - **状态标识**: 通过颜色区分已完成、进行中和待执行的节点。

---

## 2. 技术实现逻辑

### 前端技术栈

- **框架**: React (Ant Design Pro)
- **核心组件**: `antd` 的 `Calendar`、`List`、`Timeline`、`Badge`。
- **状态管理**: 使用 `dayjs` 处理日期逻辑，结合 `useState` 管理当前选中的日期和过滤状态。

### 核心代码结构

1. **`ProductionPlan/index.tsx`**: 主页面容器。
2. **`components/PlanStats.tsx`**: 顶部统计组件。
3. **`components/PlanCalendar.tsx`**: 日历视图组件。
4. **`components/TaskCenter.tsx`**: 右侧任务执行列表。
5. **`components/PlanDetailTimeline.tsx`**: 详细计划时间轴。

### 后端接口

1. **获取日历任务摘要（用于左侧日历标记）**
   `GET /api/plans/calendar?month=2026-03`
   返回字段：日期为 key，值为当日任务类型数组（如 `["feeding", "medication"]`）
   ✅ 用于在日历中渲染不同颜色的标记（🔵 投喂、💊 用药等）
2. **获取某日详细任务列表（用于右侧任务中心 + 顶部统计）**
   `GET /api/plans/tasks?date=2026-03-27`
   返回字段：`id`, `pond`, `type`, `title`, `scheduledTime`, `status`（pending/completed/overdue）, `executor`
   ✅ 用于展示任务详情、执行状态及顶部统计（待办/完成/逾期/进行中）
3. **确认任务执行**
   `POST /api/plans/tasks/{id}/complete`
   ✅ 用于一键标记任务完成，前端自动刷新当日任务列表与日历视图

### 数据流转流程

1. **日历渲染**: 根据后端返回的月份任务摘要，在日历单元格内渲染对应的标记。
2. **任务联动**: 用户点击日历中的某一天 -> 触发 `onSelect` -> 更新 `selectedDate` -> 异步拉取该日的详细任务数据。
3. **执行状态同步**: 用户点击“确认执行” -> 调用执行接口 -> 刷新当前列表并同步更新日历标记。

---

## 3. 专家理由与业务价值

- **规范化流程**: 将养殖经验转化为标准化的“生产模板”，降低对个人经验的依赖。
- **防止遗漏**: 关键任务（如定时投喂、定期防疫）通过日历和待办提醒，极大减少了人为疏忽带来的损失。
- **可追溯性**: 每一项计划的执行都有记录，为后续的质量溯源和成本核算提供基础数据。

---

该页面旨在为养殖户和基地管理者提供一个清晰的生产作业视图，帮助其科学规划、按时执行并追踪各项生产任务（如投喂、用药、换水、捕捞等）。

# 经营分析页面设计方案

该页面旨在为老板和财务主管提供全局的经营概览，通过财务数据（收入、支出、利润）与生产数据（产量、饲料转化率）的交叉分析，辅助决策，提升盈利能力。

## 1. 页面布局设计

**顶部: “行情行情”看板 (Market Ticker Cards)**

- **样式**: 取消卡片的圆角和阴影，改用硬朗的边框和深色调背景（或极简白）。
- **内容**:
  - 数值放大展示，使用等宽字体。
  - 趋势百分比置于数值右侧，并配以“闪烁”感的箭头图标。
  - 底部进度条增加分段（Segmented），模拟“业绩水位线”。

**中部: 专业交易级图表 (Advanced Trading Chart)**

- **图表类型**: 结合月度利润柱状图与收支双曲线。
- **增强功能**:
  - **十字准星 (Crosshair)**: 鼠标悬停时显示精确的水平和垂直参考线。
  - **数据网格 (Grid Lines)**: 增加细密的虚线网格，提升专业感。
  - **渐变填充 (Area Shading)**: 在曲线下方增加微妙的渐变填充，区分盈利空间。

**底部: 资产配置与业绩排行榜 (Allocation & Leaderboard)**

- **左/中 (分类占比)**:
  - 使用更精细的“空心环形图 (Donut Chart)”。
  - 中心展示“总额”，侧边列出带有 Top5 详细数值和涨跌对比的图例。
- **右侧 (基地综合效益)**:
  - **重构为表格排行榜**: 而非单纯的条形图。
  - 每一行包含：基地名、实时产量（数值）、利润（迷你柱状图 Sparkline）、ROI（红绿对比条）。

---

## 2. 技术实现逻辑

### 前端技术栈

- **框架**: React (Ant Design Pro)
- **图表库**: `echarts-for-react` (处理复杂的双轴图和复合饼图)
- **数据处理**: 使用 `Big.js` 或类似库处理财务金额的精度问题。

### 核心代码结构

1. **`BusinessAnalysis/index.tsx`**: 主页面容器。
2. **`components/BusinessStats.tsx`**: 顶部 KPI 指标卡组件。
3. **`components/ProfitTrendChart.tsx`**: 盈利趋势分析图表。
4. **`components/CategoryDistribution.tsx`**: 成本/收入占比饼图。
5. **`components/BaseRanking.tsx`**: 基地效益排名组件。
6. 

### 后端接口

1. **获取经营核心指标（KPI 卡片）**
   `GET /api/finance/kpis?period=year&baseId=optional`
   返回字段：`income`（总收入）、`cost`（总成本）、`profit`（净利润）、`roi`（投入产出比）、`prevPeriodChange`（环比/同比变化率）
   ✅ 用于顶部 KPI 指标卡，支持按年度/季度/自定义周期及单基地筛选
2. **获取月度盈利与成本趋势数据**
   `GET /api/finance/trends?range=last12months&baseId=optional`
   返回字段：时间序列数组，每项包含 `date`（如 "2026-03"）、`income`、`cost`、`profit`
   ✅ 用于中间双轴折线图，展示收入、支出与利润趋势，支持点击下钻
3. **获取某月成本与收入构成明细**
   `GET /api/finance/breakdown?date=2026-03&baseId=optional`
   返回字段：

* `costBreakdown`: 数组，含 `category`（如 "feed", "medicine"）、`amount`
* `incomeBreakdown`: 数组，含 `category`（如 "grass_carp", "shrimp"）、`amount`
  ✅ 用于底部左右两个饼图（投入品占比 + 品种收入占比）

4. **获取基地产量与利润排名**
   `GET /api/finance/ranking?date=2026-03`
   返回字段：数组，每项含 `baseName`、`production`（产量）、`profit`（利润）
   ✅ 用于底部右侧柱状图，直观对比各基地效益

### 数据流转流程

1. **聚合统计**: 后端返回按时间、基地、品种聚合后的财务与生产数据。
2. **动态下钻**: 用户在趋势图中点击某个月份 -> 触发状态更新 `selectedMonth` -> 占比图表和排名列表局部刷新。
3. **多维筛选**: 支持按“全基地/单基地”、“年度/季度/自定义时间”进行全局数据过滤。

---

## 专家理由与业务价值

- **一眼看盈亏**: 将复杂的 Excel 财务报表转化为直观的红绿指标和趋势图，老板 10 秒钟即可了解公司是否赚钱。
- **成本压降**: 通过“投入品占比”发现成本黑洞（如某基地药品开支异常偏高），针对性进行精细化管理。
- **优胜劣汰**: 基地排名让不同管理者之间产生竞争，同时为资源分配（如扩建、减产）提供客观依据。



## 技术实现细节

### **全局样式注入 (global.less)**

- 引入等宽字体用于所有数字。
- 定义通用的 `.fin-card` 类，统一样式。

### **ECharts 深度定制**

- 使用 `splitLine` 和 `axisPointer` 的详细配置实现“交易员”级别的图表交互。
- 引入 `graphic` 元素在图表角角落展示实时汇总。

---

### 为什么引入全局样式 (`global.less`)？

为了实现真正的“金融终端感”，我们必须在底层注入统一的视觉基因：

1. **确保数字的“严丝合缝” (Tabular Nums)**:

   - 金融软件的核心是数据对比。普通字体中数字宽度不一，会导致多行数据小数点对不齐。
   - **理由**: 通过全局 `.fin-number` 强制使用等宽字体，确保千分位、小数点在垂直方向完美对齐，方便老板一眼看出量级差异。
2. **统一“红涨绿跌”的业务语义**:

   - 中国金融习惯与通用 UI 框架默认的“绿好红坏”不同。
   - **理由**: 在全局定义 `.fin-trend-up` (红) 和 `.fin-trend-down` (绿)，统一全站所有卡片、图表、表格的色彩语言，降低管理者的认知成本。
3. **打造“终端级”的高信息密度**:

   - 普通网页追求留白，金融终端追求效率。
   - **理由**: 通过全局 `.fin-card` 压缩边距 (Padding)、去掉圆角和阴影，改用硬朗的细边框。这种“去装饰化”处理能让屏幕在同样大小下展示更多维度的决策数据。
4. **维护性与 DRY 原则**:

   - **理由**: 避免在每个组件中重复编写字体、颜色和边框代码。若未来需要调整“中国红”的色值，只需在 `global.less` 修改一处，全站即可同步更新。


# 综合预警页面设计方案 (金融终端风格)

该页面作为养殖系统的“指挥中心”，采用金融交易终端的高信息密度风格，旨在帮助管理人员在最短时间内发现风险、评估严重程度并快速下达处理指令。

## 1. 页面布局设计

页面采用“多维监控看板”结构，强调时效性与响应速度：

- **顶部 (15% 高度): 实时预警行情条 (Alert Tickers)**

  - **样式**: 紧凑的 KPI 卡片，使用等宽字体。
  - **内容**:
    - 🚨 **未处理预警总数**: 醒目红色高亮。
    - ⚡ **近 1 小时新增**: 模拟“涨跌幅”显示新增频率。
    - ✅ **今日已处理**: 绿色表示系统运行效率。
    - ⏱️ **平均响应时间**: 衡量团队执行力。
- **左侧 (65% 宽度): 实时预警流水 (Real-time Alert Stream)**

  - **样式**: 高密度表格（Table），取消冗余边距。
  - **核心字段**: 级别（P0/P1/P2）、发生时间（精确到秒）、来源（基地/塘口）、预警描述（如：溶氧 3.2 ↓）、持续时长。
  - **交互**:
    - **快捷操作**: 每一行带有“立即处理”、“误报忽略”等按钮。
    - **联动**: 点击行，右侧图表自动展示该指标的历史波动。
- **右侧 (35% 宽度): 风险分布与诊断 (Risk Diagnosis)**

  - **上方: 风险热力分布 (Heatmap)**: 展示各基地/塘口的风险密集度。
  - **下方: 预警类型占比 (Donut Chart)**: 水质异常、设备离线、气象灾害等类别的构成分析。
- **底部 (可选/联动展示): 故障回溯分析 (Drill-down Analysis)**

  - **ECharts 时序图**: 展示报警点前后的数据曲线，辅助判断是否为传感器误差或真实风险。

---

## 2. 视觉与交互规范 (金融风格注入)

- **字体**: 数值区域强制使用 `fin-number` (Monospaced)。
- **色彩语义**:
  - **P0 (紧急)**: 🔴 深红背景 + 白色文字（高闪烁感）。
  - **P1 (预警)**: 🟠 橙色文字。
  - **P2 (提示)**: 🟡 黄色文字。
- **布局**: 使用 `.fin-card` 统一样式，硬朗边框，零阴影。

---

## 3. 技术实现逻辑

### 核心代码结构

1. **`Comprehensive/index.tsx`**: 主页面容器，管理 WebSocket 连接或轮询状态。
2. **`components/AlertSummary.tsx`**: 顶部统计卡片。
3. **`components/RealTimeAlertList.tsx`**: 实时流水表格。
4. **`components/RiskDistribution.tsx`**: 饼图与热力图组件。

### 业务价值

- **秒级响应**: 模拟金融交易的即时感，确保重大风险（如大面积缺氧）能在秒级内触达决策层。
- **精细分级**: 避免信息轰炸，通过 P0-P2 分级让管理人员聚焦核心矛盾。
- **数据闭环**: 从报警产生到处理反馈，全流程留痕，支持后续的风险复盘。


# 预警记录页面设计方案 (金融终端风格)

该页面作为系统的“历史档案库”，旨在提供对过去所有预警事件的深度检索、统计与复盘分析。设计上延续金融软件的高信息密度与专业感。

## 1. 页面布局设计

页面采用“筛选控制 + 统计摘要 + 明细流水”的结构：

- **顶部 (可选/折叠): 高级筛选工具栏 (Advanced Filter Bar)**

  - **样式**: 紧凑型排列，减少垂直空间占用。
  - **功能**:
    - 📅 **时间维度**: 支持常用快捷键（今日、近7日、近30日、自定义范围）。
    - 🔍 **多维过滤**: 基地/塘口联动选择、预警级别（P0-P2）、处理状态（已解决、误报、未处理）。
    - ⌨️ **关键词**: 全文检索预警描述。
- **中间 (10% 高度): 历史统计快照 (Records Snapshot)**

  - **内容**:
    - 📊 **总预警数**: 环比上月增减情况。
    - 🎯 **平均解决率**: 衡量管理闭环效率。
    - ⏱️ **平均处置耗时**: 评估响应速度趋势。
- **主体 (85% 宽度): 高密度历史流水表 (High-Density History Table)**

  - **样式**:
    - 严格对齐的等宽数字字体。
    - 条纹状背景区分行，增强长列表阅读舒适度。
  - **核心字段**:
    - ID（流水号）、级别（色块标识）、发生时间、恢复时间（计算持续时长）、预警内容、处理人、处理结果（备注）。
  - **交互**:
    - 点击行打开 **详情抽屉 (Detail Drawer)**。
    - 支持一键导出 CSV/Excel（模拟金融报表导出）。
- **右侧/抽屉: 预警深度复盘 (Alert Deep-Dive)**

  - **时序分析**: 关联 ECharts 图表，展示报警时刻前后的关键指标（如溶氧）曲线。
  - **操作日志**: 谁在什么时间接收了信息，谁在什么时间提交了处理方案。

---

## 2. 视觉与技术细节

- **字体**: 数值、时间、流水号强制使用 `fin-number` 类。
- **色彩**: 延续 P0(红)、P1(橙)、P2(蓝) 的分级视觉体系。
- **性能**: 针对大规模历史数据，前端采用虚拟列表或高性能分页。

---

## 3. 业务价值

- **责任追溯**: 每一条预警从触发到关闭都有完整的证据链。
- **风险画像**: 通过统计分析，发现哪些基地经常出问题，哪些时间段是高发期（如：梅雨季节的缺氧风险）。
- **流程优化**: 识别处置瓶颈，为优化预警阈值提供数据支撑。

---


# 气象监测页面设计方案 

该页面旨在为管理层和养殖技术人员提供全方位的实时气象监控与预警，采用金融软件的高信息密度风格，确保关键天气变动（如台风、暴雨、急剧降温）能第一时间转化为生产决策。

## 1. 页面布局设计

页面采用“多维气象看板”结构，结合地理位置与时间维度：

- **顶部 (10% 高度): 实时气象行情条 (Weather Tickers)**

  - **样式**: 紧凑的 KPI 滚动条，使用等宽数字。
  - **内容**:
    - 🌡️ **当前平均气温**: 含 24h 涨跌幅。
    - 💧 **平均空气湿度**: 衡量降雨概率。
    - 🌬️ **当前风力风向**: 关键防台风指标。
    - ☀️ **当日紫外线/光照**: 影响藻类光合作用的关键因子。
- **中间 (45% 高度): 基地实时气象网格 (Base Weather Matrix)**

  - **样式**: 紧凑的网格卡片，模拟“自选股”列表。
  - **内容**: 每个基地一张卡片，展示该地精确经纬度的实时天气图标、温度、气压、降水量。
  - **交互**: 点击卡片，下方图表自动切换为该基地的详细气象趋势。
- **底部 (45% 高度): 7天趋势与灾害预警 (Forecast & Alerts)**

  - **左侧 (70% 宽度): 专业级气象趋势图 (TradingView Style)**
    - **图表**: 叠加气温（折线）、降水量（柱状）与气压（面积图）。
    - **特性**: 带有十字准星，精确显示未来每一小时的预测值。
  - **右侧 (30% 宽度): 灾害预警实时看板 (Disaster Monitor)**
    - **样式**: 高亮色块列表（P0-P2 级预警）。
    - **内容**: 台风路径、暴雨预警、高温黄色预警等官方气象台同步信息。

---

## 2. 视觉风格与技术基因

- **金融风格注入**:
  - **字体**: 所有数值（温度、风速、气压）强制使用 `fin-number` (Monospaced)。
  - **图标**: 使用极简、线性的专业气象图标，避免花哨。
  - **间距**: 极窄边距（Gutter: 8px），最大化屏幕利用率。
- **动态效果**: 气压波动与降雨概率采用微动效展示，增强“实时行情”感。

---

## 3. 业务价值

- **风险前置**: 水产养殖受天气影响极大。通过 24h 降水预测和风力监控，提前决定是否需要加固大棚或开启增氧机。
- **环境溯源**: 结合气象数据与水质记录（如暴雨后 PH 值变化），优化养殖方案。
- **智能排产**: 依据光照预测，科学安排投喂量和用药计划。

---

**请确认以上设计方案。确认后我将为您开始编写“气象监测”页面的代码实现。**
